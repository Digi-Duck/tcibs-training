const box = document.querySelectorAll(".box")

function main(){
    
    box.forEach(element => {
        body.addEventListener('keydown', 
            function(element){
            
            }
        , false)
        switch(element){
            case 65:
        }
    });
}